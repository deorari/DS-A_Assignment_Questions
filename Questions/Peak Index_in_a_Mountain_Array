class Solution {
public:
    int peakIndexInMountainArray(vector<int>& a) {
        int x;
        for(int i=0;i<a.size()-1;i++)
        {
            if(a[i]>a[i+1])
            {
                x=i;
                break;
            }
        }
        return x;
    }
};
